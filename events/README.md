# Event pages

Add each event in its own directory:

```text
events/
  event-name/
    index.html
    assets/
```

Use relative links or root-relative links that include the repository base path when the site is hosted as a GitHub project page.
